# Work suite

The current suite includes the following projects:

* https://github.com/invap/rt-monitor.git
* https://github.com/invap/dap-rt-reporter.git

This includes the current project that allows the user to report an experiment trace given its description.
And the rt-monitor which uses the dap-rt-reporter output to verify that the properties specified are valid.